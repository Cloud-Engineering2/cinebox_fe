http {
    # 기본 설정

    server {
        listen 80;
        server_name localhost;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri /index.html;
        }

        # 다른 경로들이 필요하면 여기에 추가
    }

    # 다른 서버 블록이 필요한 경우 여기에 추가
}
